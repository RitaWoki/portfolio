<%= render('layouts/errors', :object => @project) %>

  <%= form_for [@skill, @project] do |f| %>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <p><%= f.submit %></p>
  <% end %>