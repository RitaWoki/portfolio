<%= render('layouts/errors', :object => @project) %>

  <%= form_for [@skill, @project],remote: true do |f| %>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <p><%= f.submit :class => "btn btn-primary" %></p>
  <% end %>