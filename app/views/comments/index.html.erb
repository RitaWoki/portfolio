<h1>Comments</h1>

  <ul>
    <% @comments.each do |comment| %>
      <li><%= link_to comment.text, comment_new_path(comment) %></li>
    <% end %>
  </ul>

   <hr>

  <%= link_to "New comment", new_comment_path %>
  